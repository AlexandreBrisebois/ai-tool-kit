{
  "name": "generate_gemini_image",
  "description": "Generate an image using Gemini/Imagen with an optional vision critique loop.",
  "parameters": {
    "type": "object",
    "properties": {
      "prompt_or_file": {
        "type": "string",
        "description": "Text prompt or path to a Markdown file with 'image_prompt' from frontmatter."
      },
      "output": {
        "type": "string",
        "description": "Path to save the generated image (e.g. 'output/gen.webp').",
        "default": "output/generated.webp"
      },
      "no_critique": {
        "type": "boolean",
        "description": "Skip the vision feedback loop.",
        "default": false
      },
      "force": {
        "type": "boolean",
        "description": "Overwrite existing files.",
        "default": false
      },
      "json": {
        "type": "boolean",
        "description": "Output machine-readable JSON status.",
        "default": true
      }
    },
    "required": ["prompt_or_file", "output"]
  }
}
